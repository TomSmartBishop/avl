{% macro fun(type, name) %}
	{% if type=='h' %}
	{{caller()|split('\n')|first}};
	{% elif type=='inl' %}
	{{caller()|split("avl_test")|first}};
	{% elif type=='test' %}
	SECTION(name)
	{{caller()|split("avl_test")|last}};
	{% endif %}
{% endmacro %}